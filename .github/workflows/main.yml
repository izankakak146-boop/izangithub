name: RDP-Sakti-V20
on:
  workflow_dispatch:
    inputs:
      token:
        description: 'Tempel Token Ngrok'
        required: true
jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Gas
      shell: pwsh
      run: |
        net user runneradmin Sakti123! /add
        net localgroup administrators runneradmin /add
        choco install ngrok -y
        ngrok config add-authtoken ${{ github.event.inputs.token }}
        # Jalankan Ngrok dan biarkan saja, jangan dipaksa ambil IP biar gak error
        Start-Process ngrok -ArgumentList "tcp --region ap 3389"
        Write-Host "MESIN SUDAH NYALA! JANGAN LIHAT LOG LAGI!"
        while($true){ Start-Sleep 60 }
        
