name: RDP-PINGGY-MURNI
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Jalankan-Pinggy
      shell: pwsh
      run: |
        Write-Host "=============================="
        Write-Host "MEMBUKA JALUR TANPA BIKIN USER"
        Write-Host "=============================="
        
        # Langsung tembak port RDP (3389)
        ssh -o StrictHostKeyChecking=no -p 443 -R0:localhost:3389 a.pinggy.io
        
