name: Android9_Izan_Final_Beneran
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Amunisi Lengkap
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 novnc websockify qemu-utils
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared-linux-amd64.deb

      - name: 2. Download ISO Android 9
        run: |
          wget -O android9.iso "https://downloads.sourceforge.net/project/android-x86/Release%209.0/android-x86_64-9.0-r2.iso"

      - name: 3. Goreng Mesin (Pakai Perintah -append yang Benar)
        run: |
          qemu-img create -f qcow2 android.qcow2 10G
          
          # -append adalah perintah yang benar, bukan -kernel-cmdline
          sudo qemu-system-x86_64 \
            -m 8G \
            -smp 2 \
            -cpu host \
            -enable-kvm \
            -drive file=android.qcow2,format=qcow2 \
            -cdrom android9.iso \
            -device usb-ehci,id=usbbus \
            -device usb-tablet,bus=usbbus.0 \
            -vnc 0.0.0.0:0 \
            -vga std \
            -display none &

          sleep 10
          # Websockify dengerin semua IP (0.0.0.0) biar gak mental
          websockify --web /usr/share/novnc/ 6080 0.0.0.0:5900 &
          
          echo "✅ Mesin lagi proses booting..."
          sleep 60

      - name: 4. Sajikan Link VVIP
        run: |
          echo "-------------------------------------------------------"
          echo "🚀 IZAN, COBA LAGI SEKARANG!"
          echo "Gak bakal ada error 'Invalid Option' lagi."
          echo "-------------------------------------------------------"
          cloudflared tunnel --url http://127.0.0.1:6080
          
