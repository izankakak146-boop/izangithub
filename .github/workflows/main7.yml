name: Android 6H Smooth Performance
on: 
  workflow_dispatch:

jobs:
  run-android:
    runs-on: ubuntu-latest
    steps:
      - name: Install Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y x11vnc xvfb adb scrcpy jq
          sudo npm install -g localtunnel

      - name: 🧹 SAPU BERSIH HANTU EMULATOR
        run: |
          # Hapus sisa-sisa run sebelumnya biar gak bentrok
          docker rm -f $(docker ps -aq) || true
          adb kill-server || true

      - name: Launch Android (CPU Tanpa Batas)
        run: |
          # Kita lepas limit CPU-nya biar gak ngeframe
          docker run -d --privileged \
            --name android_lancar \
            -p 5555:5555 \
            redroid/redroid:8.1.0-latest \
            androidboot.redroid_width=480 \
            androidboot.redroid_height=854 \
            androidboot.redroid_fps=30 \
            redroid.gpu.mode=guest

      - name: Force Connection (High Priority)
        run: |
          Xvfb :1 -screen 0 480x854x16 &
          sleep 15
          adb start-server
          
          # Coba konek paksa ke port 5555
          until adb connect localhost:5555; do
            echo "Masih usaha konek..."
            sleep 2
          done
          
          adb -s localhost:5555 wait-for-device
          # Naikin FPS ke 30 biar smooth
          DISPLAY=:1 scrcpy -s localhost:5555 --max-fps 30 --bit-rate 4M --always-on-top &
          sleep 5
          x11vnc -display :1 -nopw -listen localhost -forever -bg -rfbport 5900

      - name: Buka Jalur Localtunnel
        run: |
          echo "===================================================="
          echo "PASSWORD IP (CEK DULU SEBELUM BUKA VNC):"
          curl -s https://loca.lt/mytunnelpassword
          echo -e "\n===================================================="
          nohup lt --port 5900 > lt.log 2>&1 &
          sleep 10
          echo "ALAMAT UNTUK RVNC VIEWER:"
          cat lt.log | grep -o 'https://[^ ]*'
          echo "===================================================="

      - name: 🕒 STANDBY 6 JAM
        run: sleep 21600
        
